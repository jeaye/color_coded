#!/usr/bin/env bash

set -euo pipefail

here="$(cd "$(dirname "${BASH_SOURCE[0]}")" && pwd)"

LD_LIBRARY_PATH="$here/../build-latest/clang-llvm/lib:$LD_LIBRARY_PATH" exec "$here/../target/debug/color_coded" "$@"
