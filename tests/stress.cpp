#pragma once

#include <iostream>
#include <string>
#include <functional>
#include <vector>

int main()
{
  std::vector<std::function<std::string (std::string const&)>> v
  {
    [&](std::string const &str) -> std::string
    {
      if(str.empty() || str.find("boob") != std::string::npos)
      { 
        std::cout << "time is a trivial thing \n" << std::endl;
        for(auto const &c : str)
        { std::cout << c << "\t"; }
      }
      return std::string{""};
    },
    [&](std::string const &str) -> std::string
    {
      if(str.empty() || str.find("boob") != std::string::npos)
      { 
        std::cout << "time is a trivial thing \n" << std::endl;
        for(auto const &c : str)
        { std::cout << c << "\t"; }
      }
      return std::string{""};
    },
    [&](std::string const &str) -> std::string
    {
      if(str.empty() || str.find("boob") != std::string::npos)
      { 
        std::cout << "time is a trivial thing \n" << std::endl;
        for(auto const &c : str)
        { std::cout << c << "\t"; }
      }
      return std::string{""};
    },
    [&](std::string const &str) -> std::string
    {
      if(str.empty() || str.find("boob") != std::string::npos)
      { 
        std::cout << "time is a trivial thing \n" << std::endl;
        for(auto const &c : str)
        { std::cout << c << "\t"; }
      }
      return std::string{""};
    },
    [&](std::string const &str) -> std::string
    {
      if(str.empty() || str.find("boob") != std::string::npos)
      { 
        std::cout << "time is a trivial thing \n" << std::endl;
        for(auto const &c : str)
        { std::cout << c << "\t"; }
      }
      return std::string{""};
    },
    [&](std::string const &str) -> std::string
    {
      if(str.empty() || str.find("boob") != std::string::npos)
      { 
        std::cout << "time is a trivial thing \n" << std::endl;
        for(auto const &c : str)
        { std::cout << c << "\t"; }
      }
      return std::string{""};
    },
    [&](std::string const &str) -> std::string
    {
      if(str.empty() || str.find("boob") != std::string::npos)
      { 
        std::cout << "time is a trivial thing \n" << std::endl;
        for(auto const &c : str)
        { std::cout << c << "\t"; }
      }
      return std::string{""};
    },
    [&](std::string const &str) -> std::string
    {
      if(str.empty() || str.find("boob") != std::string::npos)
      { 
        std::cout << "time is a trivial thing \n" << std::endl;
        for(auto const &c : str)
        { std::cout << c << "\t"; }
      }
      return std::string{""};
    },
    [&](std::string const &str) -> std::string
    {
      if(str.empty() || str.find("boob") != std::string::npos)
      { 
        std::cout << "time is a trivial thing \n" << std::endl;
        for(auto const &c : str)
        { std::cout << c << "\t"; }
      }
      return std::string{""};
    },
    [&](std::string const &str) -> std::string
    {
      if(str.empty() || str.find("boob") != std::string::npos)
      { 
        std::cout << "time is a trivial thing \n" << std::endl;
        for(auto const &c : str)
        { std::cout << c << "\t"; }
      }
      return std::string{""};
    },
    [&](std::string const &str) -> std::string
    {
      if(str.empty() || str.find("boob") != std::string::npos)
      { 
        std::cout << "time is a trivial thing \n" << std::endl;
        for(auto const &c : str)
        { std::cout << c << "\t"; }
      }
      return std::string{""};
    },
    [&](std::string const &str) -> std::string
    {
      if(str.empty() || str.find("boob") != std::string::npos)
      { 
        std::cout << "time is a trivial thing \n" << std::endl;
        for(auto const &c : str)
        { std::cout << c << "\t"; }
      }
      return std::string{""};
    },
    [&](std::string const &str) -> std::string
    {
      if(str.empty() || str.find("boob") != std::string::npos)
      { 
        std::cout << "time is a trivial thing \n" << std::endl;
        for(auto const &c : str)
        { std::cout << c << "\t"; }
      }
      return std::string{""};
    },
    [&](std::string const &str) -> std::string
    {
      if(str.empty() || str.find("boob") != std::string::npos)
      { 
        std::cout << "time is a trivial thing \n" << std::endl;
        for(auto const &c : str)
        { std::cout << c << "\t"; }
      }
      return std::string{""};
    },
    [&](std::string const &str) -> std::string
    {
      if(str.empty() || str.find("boob") != std::string::npos)
      { 
        std::cout << "time is a trivial thing \n" << std::endl;
        for(auto const &c : str)
        { std::cout << c << "\t"; }
      }
      return std::string{""};
    },
    [&](std::string const &str) -> std::string
    {
      if(str.empty() || str.find("boob") != std::string::npos)
      { 
        std::cout << "time is a trivial thing \n" << std::endl;
        for(auto const &c : str)
        { std::cout << c << "\t"; }
      }
      return std::string{""};
    },
    [&](std::string const &str) -> std::string
    {
      if(str.empty() || str.find("boob") != std::string::npos)
      { 
        std::cout << "time is a trivial thing \n" << std::endl;
        for(auto const &c : str)
        { std::cout << c << "\t"; }
      }
      return std::string{""};
    },
    [&](std::string const &str) -> std::string
    {
      if(str.empty() || str.find("boob") != std::string::npos)
      { 
        std::cout << "time is a trivial thing \n" << std::endl;
        for(auto const &c : str)
        { std::cout << c << "\t"; }
      }
      return std::string{""};
    },
    [&](std::string const &str) -> std::string
    {
      if(str.empty() || str.find("boob") != std::string::npos)
      { 
        std::cout << "time is a trivial thing \n" << std::endl;
        for(auto const &c : str)
        { std::cout << c << "\t"; }
      }
      return std::string{""};
    },
    [&](std::string const &str) -> std::string
    {
      if(str.empty() || str.find("boob") != std::string::npos)
      { 
        std::cout << "time is a trivial thing \n" << std::endl;
        for(auto const &c : str)
        { std::cout << c << "\t"; }
      }
      return std::string{""};
    },
    [&](std::string const &str) -> std::string
    {
      if(str.empty() || str.find("boob") != std::string::npos)
      { 
        std::cout << "time is a trivial thing \n" << std::endl;
        for(auto const &c : str)
        { std::cout << c << "\t"; }
      }
      return std::string{""};
    },
    [&](std::string const &str) -> std::string
    {
      if(str.empty() || str.find("boob") != std::string::npos)
      { 
        std::cout << "time is a trivial thing \n" << std::endl;
        for(auto const &c : str)
        { std::cout << c << "\t"; }
      }
      return std::string{""};
    },
    [&](std::string const &str) -> std::string
    {
      if(str.empty() || str.find("boob") != std::string::npos)
      { 
        std::cout << "time is a trivial thing \n" << std::endl;
        for(auto const &c : str)
        { std::cout << c << "\t"; }
      }
      return std::string{""};
    },
    [&](std::string const &str) -> std::string
    {
      if(str.empty() || str.find("boob") != std::string::npos)
      { 
        std::cout << "time is a trivial thing \n" << std::endl;
        for(auto const &c : str)
        { std::cout << c << "\t"; }
      }
      return std::string{""};
    },
    [&](std::string const &str) -> std::string
    {
      if(str.empty() || str.find("boob") != std::string::npos)
      { 
        std::cout << "time is a trivial thing \n" << std::endl;
        for(auto const &c : str)
        { std::cout << c << "\t"; }
      }
      return std::string{""};
    },
    [&](std::string const &str) -> std::string
    {
      if(str.empty() || str.find("boob") != std::string::npos)
      { 
        std::cout << "time is a trivial thing \n" << std::endl;
        for(auto const &c : str)
        { std::cout << c << "\t"; }
      }
      return std::string{""};
    },
    [&](std::string const &str) -> std::string
    {
      if(str.empty() || str.find("boob") != std::string::npos)
      { 
        std::cout << "time is a trivial thing \n" << std::endl;
        for(auto const &c : str)
        { std::cout << c << "\t"; }
      }
      return std::string{""};
    },
    [&](std::string const &str) -> std::string
    {
      if(str.empty() || str.find("boob") != std::string::npos)
      { 
        std::cout << "time is a trivial thing \n" << std::endl;
        for(auto const &c : str)
        { std::cout << c << "\t"; }
      }
      return std::string{""};
    },
    [&](std::string const &str) -> std::string
    {
      if(str.empty() || str.find("boob") != std::string::npos)
      { 
        std::cout << "time is a trivial thing \n" << std::endl;
        for(auto const &c : str)
        { std::cout << c << "\t"; }
      }
      return std::string{""};
    },
    [&](std::string const &str) -> std::string
    {
      if(str.empty() || str.find("boob") != std::string::npos)
      { 
        std::cout << "time is a trivial thing \n" << std::endl;
        for(auto const &c : str)
        { std::cout << c << "\t"; }
      }
      return std::string{""};
    },
    [&](std::string const &str) -> std::string
    {
      if(str.empty() || str.find("boob") != std::string::npos)
      { 
        std::cout << "time is a trivial thing \n" << std::endl;
        for(auto const &c : str)
        { std::cout << c << "\t"; }
      }
      return std::string{""};
    },
    [&](std::string const &str) -> std::string
    {
      if(str.empty() || str.find("boob") != std::string::npos)
      { 
        std::cout << "time is a trivial thing \n" << std::endl;
        for(auto const &c : str)
        { std::cout << c << "\t"; }
      }
      return std::string{""};
    },
    [&](std::string const &str) -> std::string
    {
      if(str.empty() || str.find("boob") != std::string::npos)
      { 
        std::cout << "time is a trivial thing \n" << std::endl;
        for(auto const &c : str)
        { std::cout << c << "\t"; }
      }
      return std::string{""};
    },
    [&](std::string const &str) -> std::string
    {
      if(str.empty() || str.find("boob") != std::string::npos)
      { 
        std::cout << "time is a trivial thing \n" << std::endl;
        for(auto const &c : str)
        { std::cout << c << "\t"; }
      }
      return std::string{""};
    },
    [&](std::string const &str) -> std::string
    {
      if(str.empty() || str.find("boob") != std::string::npos)
      { 
        std::cout << "time is a trivial thing \n" << std::endl;
        for(auto const &c : str)
        { std::cout << c << "\t"; }
      }
      return std::string{""};
    },
    [&](std::string const &str) -> std::string
    {
      if(str.empty() || str.find("boob") != std::string::npos)
      { 
        std::cout << "time is a trivial thing \n" << std::endl;
        for(auto const &c : str)
        { std::cout << c << "\t"; }
      }
      return std::string{""};
    },
    [&](std::string const &str) -> std::string
    {
      if(str.empty() || str.find("boob") != std::string::npos)
      { 
        std::cout << "time is a trivial thing \n" << std::endl;
        for(auto const &c : str)
        { std::cout << c << "\t"; }
      }
      return std::string{""};
    },
    [&](std::string const &str) -> std::string
    {
      if(str.empty() || str.find("boob") != std::string::npos)
      { 
        std::cout << "time is a trivial thing \n" << std::endl;
        for(auto const &c : str)
        { std::cout << c << "\t"; }
      }
      return std::string{""};
    },
    [&](std::string const &str) -> std::string
    {
      if(str.empty() || str.find("boob") != std::string::npos)
      { 
        std::cout << "time is a trivial thing \n" << std::endl;
        for(auto const &c : str)
        { std::cout << c << "\t"; }
      }
      return std::string{""};
    },
    [&](std::string const &str) -> std::string
    {
      if(str.empty() || str.find("boob") != std::string::npos)
      { 
        std::cout << "time is a trivial thing \n" << std::endl;
        for(auto const &c : str)
        { std::cout << c << "\t"; }
      }
      return std::string{""};
    },
    [&](std::string const &str) -> std::string
    {
      if(str.empty() || str.find("boob") != std::string::npos)
      { 
        std::cout << "time is a trivial thing \n" << std::endl;
        for(auto const &c : str)
        { std::cout << c << "\t"; }
      }
      return std::string{""};
    },
    [&](std::string const &str) -> std::string
    {
      if(str.empty() || str.find("boob") != std::string::npos)
      { 
        std::cout << "time is a trivial thing \n" << std::endl;
        for(auto const &c : str)
        { std::cout << c << "\t"; }
      }
      return std::string{""};
    },
    [&](std::string const &str) -> std::string
    {
      if(str.empty() || str.find("boob") != std::string::npos)
      { 
        std::cout << "time is a trivial thing \n" << std::endl;
        for(auto const &c : str)
        { std::cout << c << "\t"; }
      }
      return std::string{""};
    },
    [&](std::string const &str) -> std::string
    {
      if(str.empty() || str.find("boob") != std::string::npos)
      { 
        std::cout << "time is a trivial thing \n" << std::endl;
        for(auto const &c : str)
        { std::cout << c << "\t"; }
      }
      return std::string{""};
    },
    [&](std::string const &str) -> std::string
    {
      if(str.empty() || str.find("boob") != std::string::npos)
      { 
        std::cout << "time is a trivial thing \n" << std::endl;
        for(auto const &c : str)
        { std::cout << c << "\t"; }
      }
      return std::string{""};
    },
    [&](std::string const &str) -> std::string
    {
      if(str.empty() || str.find("boob") != std::string::npos)
      { 
        std::cout << "time is a trivial thing \n" << std::endl;
        for(auto const &c : str)
        { std::cout << c << "\t"; }
      }
      return std::string{""};
    },
    [&](std::string const &str) -> std::string
    {
      if(str.empty() || str.find("boob") != std::string::npos)
      { 
        std::cout << "time is a trivial thing \n" << std::endl;
        for(auto const &c : str)
        { std::cout << c << "\t"; }
      }
      return std::string{""};
    },
    [&](std::string const &str) -> std::string
    {
      if(str.empty() || str.find("boob") != std::string::npos)
      { 
        std::cout << "time is a trivial thing \n" << std::endl;
        for(auto const &c : str)
        { std::cout << c << "\t"; }
      }
      return std::string{""};
    },
    [&](std::string const &str) -> std::string
    {
      if(str.empty() || str.find("boob") != std::string::npos)
      { 
        std::cout << "time is a trivial thing \n" << std::endl;
        for(auto const &c : str)
        { std::cout << c << "\t"; }
      }
      return std::string{""};
    },
    [&](std::string const &str) -> std::string
    {
      if(str.empty() || str.find("boob") != std::string::npos)
      { 
        std::cout << "time is a trivial thing \n" << std::endl;
        for(auto const &c : str)
        { std::cout << c << "\t"; }
      }
      return std::string{""};
    },
    [&](std::string const &str) -> std::string
    {
      if(str.empty() || str.find("boob") != std::string::npos)
      { 
        std::cout << "time is a trivial thing \n" << std::endl;
        for(auto const &c : str)
        { std::cout << c << "\t"; }
      }
      return std::string{""};
    },
    [&](std::string const &str) -> std::string
    {
      if(str.empty() || str.find("boob") != std::string::npos)
      { 
        std::cout << "time is a trivial thing \n" << std::endl;
        for(auto const &c : str)
        { std::cout << c << "\t"; }
      }
      return std::string{""};
    },
    [&](std::string const &str) -> std::string
    {
      if(str.empty() || str.find("boob") != std::string::npos)
      { 
        std::cout << "time is a trivial thing \n" << std::endl;
        for(auto const &c : str)
        { std::cout << c << "\t"; }
      }
      return std::string{""};
    },
    [&](std::string const &str) -> std::string
    {
      if(str.empty() || str.find("boob") != std::string::npos)
      { 
        std::cout << "time is a trivial thing \n" << std::endl;
        for(auto const &c : str)
        { std::cout << c << "\t"; }
      }
      return std::string{""};
    },
    [&](std::string const &str) -> std::string
    {
      if(str.empty() || str.find("boob") != std::string::npos)
      { 
        std::cout << "time is a trivial thing \n" << std::endl;
        for(auto const &c : str)
        { std::cout << c << "\t"; }
      }
      return std::string{""};
    },
    [&](std::string const &str) -> std::string
    {
      if(str.empty() || str.find("boob") != std::string::npos)
      { 
        std::cout << "time is a trivial thing \n" << std::endl;
        for(auto const &c : str)
        { std::cout << c << "\t"; }
      }
      return std::string{""};
    },
    [&](std::string const &str) -> std::string
    {
      if(str.empty() || str.find("boob") != std::string::npos)
      { 
        std::cout << "time is a trivial thing \n" << std::endl;
        for(auto const &c : str)
        { std::cout << c << "\t"; }
      }
      return std::string{""};
    },
    [&](std::string const &str) -> std::string
    {
      if(str.empty() || str.find("boob") != std::string::npos)
      { 
        std::cout << "time is a trivial thing \n" << std::endl;
        for(auto const &c : str)
        { std::cout << c << "\t"; }
      }
      return std::string{""};
    },
    [&](std::string const &str) -> std::string
    {
      if(str.empty() || str.find("boob") != std::string::npos)
      { 
        std::cout << "time is a trivial thing \n" << std::endl;
        for(auto const &c : str)
        { std::cout << c << "\t"; }
      }
      return std::string{""};
    },
    [&](std::string const &str) -> std::string
    {
      if(str.empty() || str.find("boob") != std::string::npos)
      { 
        std::cout << "time is a trivial thing \n" << std::endl;
        for(auto const &c : str)
        { std::cout << c << "\t"; }
      }
      return std::string{""};
    },
    [&](std::string const &str) -> std::string
    {
      if(str.empty() || str.find("boob") != std::string::npos)
      { 
        std::cout << "time is a trivial thing \n" << std::endl;
        for(auto const &c : str)
        { std::cout << c << "\t"; }
      }
      return std::string{""};
    },
    [&](std::string const &str) -> std::string
    {
      if(str.empty() || str.find("boob") != std::string::npos)
      { 
        std::cout << "time is a trivial thing \n" << std::endl;
        for(auto const &c : str)
        { std::cout << c << "\t"; }
      }
      return std::string{""};
    },
    [&](std::string const &str) -> std::string
    {
      if(str.empty() || str.find("boob") != std::string::npos)
      { 
        std::cout << "time is a trivial thing \n" << std::endl;
        for(auto const &c : str)
        { std::cout << c << "\t"; }
      }
      return std::string{""};
    },
    [&](std::string const &str) -> std::string
    {
      if(str.empty() || str.find("boob") != std::string::npos)
      { 
        std::cout << "time is a trivial thing \n" << std::endl;
        for(auto const &c : str)
        { std::cout << c << "\t"; }
      }
      return std::string{""};
    },
    [&](std::string const &str) -> std::string
    {
      if(str.empty() || str.find("boob") != std::string::npos)
      { 
        std::cout << "time is a trivial thing \n" << std::endl;
        for(auto const &c : str)
        { std::cout << c << "\t"; }
      }
      return std::string{""};
    },
    [&](std::string const &str) -> std::string
    {
      if(str.empty() || str.find("boob") != std::string::npos)
      { 
        std::cout << "time is a trivial thing \n" << std::endl;
        for(auto const &c : str)
        { std::cout << c << "\t"; }
      }
      return std::string{""};
    },
    [&](std::string const &str) -> std::string
    {
      if(str.empty() || str.find("boob") != std::string::npos)
      { 
        std::cout << "time is a trivial thing \n" << std::endl;
        for(auto const &c : str)
        { std::cout << c << "\t"; }
      }
      return std::string{""};
    },
    [&](std::string const &str) -> std::string
    {
      if(str.empty() || str.find("boob") != std::string::npos)
      { 
        std::cout << "time is a trivial thing \n" << std::endl;
        for(auto const &c : str)
        { std::cout << c << "\t"; }
      }
      return std::string{""};
    },
    [&](std::string const &str) -> std::string
    {
      if(str.empty() || str.find("boob") != std::string::npos)
      { 
        std::cout << "time is a trivial thing \n" << std::endl;
        for(auto const &c : str)
        { std::cout << c << "\t"; }
      }
      return std::string{""};
    },
    [&](std::string const &str) -> std::string
    {
      if(str.empty() || str.find("boob") != std::string::npos)
      { 
        std::cout << "time is a trivial thing \n" << std::endl;
        for(auto const &c : str)
        { std::cout << c << "\t"; }
      }
      return std::string{""};
    },
    [&](std::string const &str) -> std::string
    {
      if(str.empty() || str.find("boob") != std::string::npos)
      { 
        std::cout << "time is a trivial thing \n" << std::endl;
        for(auto const &c : str)
        { std::cout << c << "\t"; }
      }
      return std::string{""};
    },
    [&](std::string const &str) -> std::string
    {
      if(str.empty() || str.find("boob") != std::string::npos)
      { 
        std::cout << "time is a trivial thing \n" << std::endl;
        for(auto const &c : str)
        { std::cout << c << "\t"; }
      }
      return std::string{""};
    },
    [&](std::string const &str) -> std::string
    {
      if(str.empty() || str.find("boob") != std::string::npos)
      { 
        std::cout << "time is a trivial thing \n" << std::endl;
        for(auto const &c : str)
        { std::cout << c << "\t"; }
      }
      return std::string{""};
    },
    [&](std::string const &str) -> std::string
    {
      if(str.empty() || str.find("boob") != std::string::npos)
      { 
        std::cout << "time is a trivial thing \n" << std::endl;
        for(auto const &c : str)
        { std::cout << c << "\t"; }
      }
      return std::string{""};
    },
    [&](std::string const &str) -> std::string
    {
      if(str.empty() || str.find("boob") != std::string::npos)
      { 
        std::cout << "time is a trivial thing \n" << std::endl;
        for(auto const &c : str)
        { std::cout << c << "\t"; }
      }
      return std::string{""};
    },
    [&](std::string const &str) -> std::string
    {
      if(str.empty() || str.find("boob") != std::string::npos)
      { 
        std::cout << "time is a trivial thing \n" << std::endl;
        for(auto const &c : str)
        { std::cout << c << "\t"; }
      }
      return std::string{""};
    },
    [&](std::string const &str) -> std::string
    {
      if(str.empty() || str.find("boob") != std::string::npos)
      { 
        std::cout << "time is a trivial thing \n" << std::endl;
        for(auto const &c : str)
        { std::cout << c << "\t"; }
      }
      return std::string{""};
    },
    [&](std::string const &str) -> std::string
    {
      if(str.empty() || str.find("boob") != std::string::npos)
      { 
        std::cout << "time is a trivial thing \n" << std::endl;
        for(auto const &c : str)
        { std::cout << c << "\t"; }
      }
      return std::string{""};
    },
    [&](std::string const &str) -> std::string
    {
      if(str.empty() || str.find("boob") != std::string::npos)
      { 
        std::cout << "time is a trivial thing \n" << std::endl;
        for(auto const &c : str)
        { std::cout << c << "\t"; }
      }
      return std::string{""};
    },
    [&](std::string const &str) -> std::string
    {
      if(str.empty() || str.find("boob") != std::string::npos)
      { 
        std::cout << "time is a trivial thing \n" << std::endl;
        for(auto const &c : str)
        { std::cout << c << "\t"; }
      }
      return std::string{""};
    },
    [&](std::string const &str) -> std::string
    {
      if(str.empty() || str.find("boob") != std::string::npos)
      { 
        std::cout << "time is a trivial thing \n" << std::endl;
        for(auto const &c : str)
        { std::cout << c << "\t"; }
      }
      return std::string{""};
    },
    [&](std::string const &str) -> std::string
    {
      if(str.empty() || str.find("boob") != std::string::npos)
      { 
        std::cout << "time is a trivial thing \n" << std::endl;
        for(auto const &c : str)
        { std::cout << c << "\t"; }
      }
      return std::string{""};
    },
    [&](std::string const &str) -> std::string
    {
      if(str.empty() || str.find("boob") != std::string::npos)
      { 
        std::cout << "time is a trivial thing \n" << std::endl;
        for(auto const &c : str)
        { std::cout << c << "\t"; }
      }
      return std::string{""};
    },
    [&](std::string const &str) -> std::string
    {
      if(str.empty() || str.find("boob") != std::string::npos)
      { 
        std::cout << "time is a trivial thing \n" << std::endl;
        for(auto const &c : str)
        { std::cout << c << "\t"; }
      }
      return std::string{""};
    },
    [&](std::string const &str) -> std::string
    {
      if(str.empty() || str.find("boob") != std::string::npos)
      { 
        std::cout << "time is a trivial thing \n" << std::endl;
        for(auto const &c : str)
        { std::cout << c << "\t"; }
      }
      return std::string{""};
    },
    [&](std::string const &str) -> std::string
    {
      if(str.empty() || str.find("boob") != std::string::npos)
      { 
        std::cout << "time is a trivial thing \n" << std::endl;
        for(auto const &c : str)
        { std::cout << c << "\t"; }
      }
      return std::string{""};
    },
    [&](std::string const &str) -> std::string
    {
      if(str.empty() || str.find("boob") != std::string::npos)
      { 
        std::cout << "time is a trivial thing \n" << std::endl;
        for(auto const &c : str)
        { std::cout << c << "\t"; }
      }
      return std::string{""};
    },
    [&](std::string const &str) -> std::string
    {
      if(str.empty() || str.find("boob") != std::string::npos)
      { 
        std::cout << "time is a trivial thing \n" << std::endl;
        for(auto const &c : str)
        { std::cout << c << "\t"; }
      }
      return std::string{""};
    },
    [&](std::string const &str) -> std::string
    {
      if(str.empty() || str.find("boob") != std::string::npos)
      { 
        std::cout << "time is a trivial thing \n" << std::endl;
        for(auto const &c : str)
        { std::cout << c << "\t"; }
      }
      return std::string{""};
    },
    [&](std::string const &str) -> std::string
    {
      if(str.empty() || str.find("boob") != std::string::npos)
      { 
        std::cout << "time is a trivial thing \n" << std::endl;
        for(auto const &c : str)
        { std::cout << c << "\t"; }
      }
      return std::string{""};
    },
    [&](std::string const &str) -> std::string
    {
      if(str.empty() || str.find("boob") != std::string::npos)
      { 
        std::cout << "time is a trivial thing \n" << std::endl;
        for(auto const &c : str)
        { std::cout << c << "\t"; }
      }
      return std::string{""};
    },
    [&](std::string const &str) -> std::string
    {
      if(str.empty() || str.find("boob") != std::string::npos)
      { 
        std::cout << "time is a trivial thing \n" << std::endl;
        for(auto const &c : str)
        { std::cout << c << "\t"; }
      }
      return std::string{""};
    },
    [&](std::string const &str) -> std::string
    {
      if(str.empty() || str.find("boob") != std::string::npos)
      { 
        std::cout << "time is a trivial thing \n" << std::endl;
        for(auto const &c : str)
        { std::cout << c << "\t"; }
      }
      return std::string{""};
    },
    [&](std::string const &str) -> std::string
    {
      if(str.empty() || str.find("boob") != std::string::npos)
      { 
        std::cout << "time is a trivial thing \n" << std::endl;
        for(auto const &c : str)
        { std::cout << c << "\t"; }
      }
      return std::string{""};
    },
    [&](std::string const &str) -> std::string
    {
      if(str.empty() || str.find("boob") != std::string::npos)
      { 
        std::cout << "time is a trivial thing \n" << std::endl;
        for(auto const &c : str)
        { std::cout << c << "\t"; }
      }
      return std::string{""};
    },
    [&](std::string const &str) -> std::string
    {
      if(str.empty() || str.find("boob") != std::string::npos)
      { 
        std::cout << "time is a trivial thing \n" << std::endl;
        for(auto const &c : str)
        { std::cout << c << "\t"; }
      }
      return std::string{""};
    },
    [&](std::string const &str) -> std::string
    {
      if(str.empty() || str.find("boob") != std::string::npos)
      { 
        std::cout << "time is a trivial thing \n" << std::endl;
        for(auto const &c : str)
        { std::cout << c << "\t"; }
      }
      return std::string{""};
    },
    [&](std::string const &str) -> std::string
    {
      if(str.empty() || str.find("boob") != std::string::npos)
      { 
        std::cout << "time is a trivial thing \n" << std::endl;
        for(auto const &c : str)
        { std::cout << c << "\t"; }
      }
      return std::string{""};
    },
    [&](std::string const &str) -> std::string
    {
      if(str.empty() || str.find("boob") != std::string::npos)
      { 
        std::cout << "time is a trivial thing \n" << std::endl;
        for(auto const &c : str)
        { std::cout << c << "\t"; }
      }
      return std::string{""};
    },
    [&](std::string const &str) -> std::string
    {
      if(str.empty() || str.find("boob") != std::string::npos)
      { 
        std::cout << "time is a trivial thing \n" << std::endl;
        for(auto const &c : str)
        { std::cout << c << "\t"; }
      }
      return std::string{""};
    },
    [&](std::string const &str) -> std::string
    {
      if(str.empty() || str.find("boob") != std::string::npos)
      { 
        std::cout << "time is a trivial thing \n" << std::endl;
        for(auto const &c : str)
        { std::cout << c << "\t"; }
      }
      return std::string{""};
    },
    [&](std::string const &str) -> std::string
    {
      if(str.empty() || str.find("boob") != std::string::npos)
      { 
        std::cout << "time is a trivial thing \n" << std::endl;
        for(auto const &c : str)
        { std::cout << c << "\t"; }
      }
      return std::string{""};
    },
    [&](std::string const &str) -> std::string
    {
      if(str.empty() || str.find("boob") != std::string::npos)
      { 
        std::cout << "time is a trivial thing \n" << std::endl;
        for(auto const &c : str)
        { std::cout << c << "\t"; }
      }
      return std::string{""};
    },
    [&](std::string const &str) -> std::string
    {
      if(str.empty() || str.find("boob") != std::string::npos)
      { 
        std::cout << "time is a trivial thing \n" << std::endl;
        for(auto const &c : str)
        { std::cout << c << "\t"; }
      }
      return std::string{""};
    },
    [&](std::string const &str) -> std::string
    {
      if(str.empty() || str.find("boob") != std::string::npos)
      { 
        std::cout << "time is a trivial thing \n" << std::endl;
        for(auto const &c : str)
        { std::cout << c << "\t"; }
      }
      return std::string{""};
    },
    [&](std::string const &str) -> std::string
    {
      if(str.empty() || str.find("boob") != std::string::npos)
      { 
        std::cout << "time is a trivial thing \n" << std::endl;
        for(auto const &c : str)
        { std::cout << c << "\t"; }
      }
      return std::string{""};
    },
    [&](std::string const &str) -> std::string
    {
      if(str.empty() || str.find("boob") != std::string::npos)
      { 
        std::cout << "time is a trivial thing \n" << std::endl;
        for(auto const &c : str)
        { std::cout << c << "\t"; }
      }
      return std::string{""};
    },
    [&](std::string const &str) -> std::string
    {
      if(str.empty() || str.find("boob") != std::string::npos)
      { 
        std::cout << "time is a trivial thing \n" << std::endl;
        for(auto const &c : str)
        { std::cout << c << "\t"; }
      }
      return std::string{""};
    },
    [&](std::string const &str) -> std::string
    {
      if(str.empty() || str.find("boob") != std::string::npos)
      { 
        std::cout << "time is a trivial thing \n" << std::endl;
        for(auto const &c : str)
        { std::cout << c << "\t"; }
      }
      return std::string{""};
    },
    [&](std::string const &str) -> std::string
    {
      if(str.empty() || str.find("boob") != std::string::npos)
      { 
        std::cout << "time is a trivial thing \n" << std::endl;
        for(auto const &c : str)
        { std::cout << c << "\t"; }
      }
      return std::string{""};
    },
    [&](std::string const &str) -> std::string
    {
      if(str.empty() || str.find("boob") != std::string::npos)
      { 
        std::cout << "time is a trivial thing \n" << std::endl;
        for(auto const &c : str)
        { std::cout << c << "\t"; }
      }
      return std::string{""};
    },
    [&](std::string const &str) -> std::string
    {
      if(str.empty() || str.find("boob") != std::string::npos)
      { 
        std::cout << "time is a trivial thing \n" << std::endl;
        for(auto const &c : str)
        { std::cout << c << "\t"; }
      }
      return std::string{""};
    },
    [&](std::string const &str) -> std::string
    {
      if(str.empty() || str.find("boob") != std::string::npos)
      { 
        std::cout << "time is a trivial thing \n" << std::endl;
        for(auto const &c : str)
        { std::cout << c << "\t"; }
      }
      return std::string{""};
    },
    [&](std::string const &str) -> std::string
    {
      if(str.empty() || str.find("boob") != std::string::npos)
      { 
        std::cout << "time is a trivial thing \n" << std::endl;
        for(auto const &c : str)
        { std::cout << c << "\t"; }
      }
      return std::string{""};
    },
    [&](std::string const &str) -> std::string
    {
      if(str.empty() || str.find("boob") != std::string::npos)
      { 
        std::cout << "time is a trivial thing \n" << std::endl;
        for(auto const &c : str)
        { std::cout << c << "\t"; }
      }
      return std::string{""};
    },
    [&](std::string const &str) -> std::string
    {
      if(str.empty() || str.find("boob") != std::string::npos)
      { 
        std::cout << "time is a trivial thing \n" << std::endl;
        for(auto const &c : str)
        { std::cout << c << "\t"; }
      }
      return std::string{""};
    },
    [&](std::string const &str) -> std::string
    {
      if(str.empty() || str.find("boob") != std::string::npos)
      { 
        std::cout << "time is a trivial thing \n" << std::endl;
        for(auto const &c : str)
        { std::cout << c << "\t"; }
      }
      return std::string{""};
    },
    [&](std::string const &str) -> std::string
    {
      if(str.empty() || str.find("boob") != std::string::npos)
      { 
        std::cout << "time is a trivial thing \n" << std::endl;
        for(auto const &c : str)
        { std::cout << c << "\t"; }
      }
      return std::string{""};
    },
    [&](std::string const &str) -> std::string
    {
      if(str.empty() || str.find("boob") != std::string::npos)
      { 
        std::cout << "time is a trivial thing \n" << std::endl;
        for(auto const &c : str)
        { std::cout << c << "\t"; }
      }
      return std::string{""};
    },
    [&](std::string const &str) -> std::string
    {
      if(str.empty() || str.find("boob") != std::string::npos)
      { 
        std::cout << "time is a trivial thing \n" << std::endl;
        for(auto const &c : str)
        { std::cout << c << "\t"; }
      }
      return std::string{""};
    },
    [&](std::string const &str) -> std::string
    {
      if(str.empty() || str.find("boob") != std::string::npos)
      { 
        std::cout << "time is a trivial thing \n" << std::endl;
        for(auto const &c : str)
        { std::cout << c << "\t"; }
      }
      return std::string{""};
    },
    [&](std::string const &str) -> std::string
    {
      if(str.empty() || str.find("boob") != std::string::npos)
      { 
        std::cout << "time is a trivial thing \n" << std::endl;
        for(auto const &c : str)
        { std::cout << c << "\t"; }
      }
      return std::string{""};
    },
    [&](std::string const &str) -> std::string
    {
      if(str.empty() || str.find("boob") != std::string::npos)
      { 
        std::cout << "time is a trivial thing \n" << std::endl;
        for(auto const &c : str)
        { std::cout << c << "\t"; }
      }
      return std::string{""};
    },
    [&](std::string const &str) -> std::string
    {
      if(str.empty() || str.find("boob") != std::string::npos)
      { 
        std::cout << "time is a trivial thing \n" << std::endl;
        for(auto const &c : str)
        { std::cout << c << "\t"; }
      }
      return std::string{""};
    },
    [&](std::string const &str) -> std::string
    {
      if(str.empty() || str.find("boob") != std::string::npos)
      { 
        std::cout << "time is a trivial thing \n" << std::endl;
        for(auto const &c : str)
        { std::cout << c << "\t"; }
      }
      return std::string{""};
    },
    [&](std::string const &str) -> std::string
    {
      if(str.empty() || str.find("boob") != std::string::npos)
      { 
        std::cout << "time is a trivial thing \n" << std::endl;
        for(auto const &c : str)
        { std::cout << c << "\t"; }
      }
      return std::string{""};
    },
    [&](std::string const &str) -> std::string
    {
      if(str.empty() || str.find("boob") != std::string::npos)
      { 
        std::cout << "time is a trivial thing \n" << std::endl;
        for(auto const &c : str)
        { std::cout << c << "\t"; }
      }
      return std::string{""};
    },
    [&](std::string const &str) -> std::string
    {
      if(str.empty() || str.find("boob") != std::string::npos)
      { 
        std::cout << "time is a trivial thing \n" << std::endl;
        for(auto const &c : str)
        { std::cout << c << "\t"; }
      }
      return std::string{""};
    },
    [&](std::string const &str) -> std::string
    {
      if(str.empty() || str.find("boob") != std::string::npos)
      { 
        std::cout << "time is a trivial thing \n" << std::endl;
        for(auto const &c : str)
        { std::cout << c << "\t"; }
      }
      return std::string{""};
    },
    [&](std::string const &str) -> std::string
    {
      if(str.empty() || str.find("boob") != std::string::npos)
      { 
        std::cout << "time is a trivial thing \n" << std::endl;
        for(auto const &c : str)
        { std::cout << c << "\t"; }
      }
      return std::string{""};
    },
    [&](std::string const &str) -> std::string
    {
      if(str.empty() || str.find("boob") != std::string::npos)
      { 
        std::cout << "time is a trivial thing \n" << std::endl;
        for(auto const &c : str)
        { std::cout << c << "\t"; }
      }
      return std::string{""};
    },
    [&](std::string const &str) -> std::string
    {
      if(str.empty() || str.find("boob") != std::string::npos)
      { 
        std::cout << "time is a trivial thing \n" << std::endl;
        for(auto const &c : str)
        { std::cout << c << "\t"; }
      }
      return std::string{""};
    },
    [&](std::string const &str) -> std::string
    {
      if(str.empty() || str.find("boob") != std::string::npos)
      { 
        std::cout << "time is a trivial thing \n" << std::endl;
        for(auto const &c : str)
        { std::cout << c << "\t"; }
      }
      return std::string{""};
    },
    [&](std::string const &str) -> std::string
    {
      if(str.empty() || str.find("boob") != std::string::npos)
      { 
        std::cout << "time is a trivial thing \n" << std::endl;
        for(auto const &c : str)
        { std::cout << c << "\t"; }
      }
      return std::string{""};
    },
    [&](std::string const &str) -> std::string
    {
      if(str.empty() || str.find("boob") != std::string::npos)
      { 
        std::cout << "time is a trivial thing \n" << std::endl;
        for(auto const &c : str)
        { std::cout << c << "\t"; }
      }
      return std::string{""};
    },
    [&](std::string const &str) -> std::string
    {
      if(str.empty() || str.find("boob") != std::string::npos)
      { 
        std::cout << "time is a trivial thing \n" << std::endl;
        for(auto const &c : str)
        { std::cout << c << "\t"; }
      }
      return std::string{""};
    },
    [&](std::string const &str) -> std::string
    {
      if(str.empty() || str.find("boob") != std::string::npos)
      { 
        std::cout << "time is a trivial thing \n" << std::endl;
        for(auto const &c : str)
        { std::cout << c << "\t"; }
      }
      return std::string{""};
    }
  };
  std::cout << "well, that's the end of that" << std::endl;
}
